[project]
name = "personal-chess-report-on-your-own-play"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Diana Angheluta" }
]
requires-python = ">=3.14"
dependencies = [
    "dotenv>=0.9.9",
    "duckdb>=1.5.5",
    "python-chess>=1.999",
    "requests>=2.34.2",
]

[project.scripts]
personal-chess-report-on-your-own-play = "personal_chess_report_on_your_own_play:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
